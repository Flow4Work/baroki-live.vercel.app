# 바로키

Windows와 Mac 단축키를 검색하고, 분류하고, 저장하고, 실제 키보드 위치로 확인할 수 있는 정적 웹 서비스입니다.

## 실행

`index.html`을 브라우저에서 열면 됩니다.

## 검증

```bash
npm run lint
npm run build
npm run test:e2e
```

저장한 단축키와 선택한 운영체제는 브라우저 `localStorage`에 보관됩니다.
